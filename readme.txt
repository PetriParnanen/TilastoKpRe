This is sports statistic app, but only the frontend done using react. This uses same back end as TilastoKp, so that needs to be started separately.

Frontend: React.JS and Bootstrap.

Will be adding redux soonish, but right now it saves things to localstorage.